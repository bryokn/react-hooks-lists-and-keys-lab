import React from "react";

function NavBar() {
  const links = ["home", "about", "projects"];

  return <nav>
    <a href="#home">Home</a>
    <a href="#home">About</a>
    <a href="#home">Projects</a>
    </nav>;
}

export default NavBar;
